/*
    state.profiles = {
        [username]: {
            userId: 0,
            photoIds: [],
            pageToken:
            followers:
            following:
            
        }
    }
*/

import * as p from './actionTypes';

const initialState = {

};

export default (state = initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}